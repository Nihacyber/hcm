rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules - allows read/write for testing
    // WARNING: Remove authentication requirement for development only
    // For production, implement proper authentication

    match /schools/{schoolId} {
      // Allow all operations for development
      allow read, write: if true;

      // Teachers subcollection
      match /teachers/{teacherId} {
        allow read, write: if true;
      }

      // Mentors subcollection
      match /mentors/{mentorId} {
        allow read, write: if true;
      }

      // Management subcollection
      match /management/{managementId} {
        allow read, write: if true;
      }
    }

    // Teachers Collection (top-level)
    match /teachers/{teacherId} {
      allow read, write: if true;
    }

    // Training Programs Collection
    match /trainingPrograms/{trainingId} {
      allow read, write: if true;
    }

    // Tasks Collection
    match /tasks/{taskId} {
      allow read, write: if true;
    }

    // Audits Collection
    match /audits/{auditId} {
      allow read, write: if true;
    }

    // User Profiles Collection
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}